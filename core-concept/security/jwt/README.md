# JWT

Exemple JWT pour la taverne, avec un scenario de livre de comptes et de coffre fort.

### [JWT - Livre de comptes et coffre fort](jwt-tutorial)
Protection des ressources sensibles via un token JWT (roles `accountant` et `treasurer`).

### [JWT + JPA - Login + Token](jwt-jpa-tutorial)
Utilisateurs en base (JPA) et generation de JWT lors du login.
